
DPATH       := $($(DOCKER_CONFIG_ENGINE_RESOLUTE)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-config-engine-resolute.mk rules/docker-config-engine-resolute.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))
DEP_FILES   += dockers/dockerfile-macros.j2

$(DOCKER_CONFIG_ENGINE_RESOLUTE)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_CONFIG_ENGINE_RESOLUTE)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_CONFIG_ENGINE_RESOLUTE)_DEP_FILES   := $(DEP_FILES)
