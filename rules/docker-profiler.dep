DPATH       := $($(DOCKER_PROFILER)_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) rules/docker-profiler.mk rules/docker-profiler.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
DEP_FILES   += $(shell git ls-files $(DPATH))

$(DOCKER_PROFILER)_CACHE_MODE  := GIT_CONTENT_SHA
$(DOCKER_PROFILER)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(DOCKER_PROFILER)_DEP_FILES   := $(DEP_FILES)

$(eval $(call add_dbg_docker,$(DOCKER_PROFILER),$(DOCKER_PROFILER_DBG)))
