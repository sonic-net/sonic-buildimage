receivers:
  otlp:
    protocols:
      grpc:
        endpoint: "127.0.0.1:4317"
      http:
        endpoint: "127.0.0.1:4318"

processors:
  batch:
    timeout: 1s
    send_batch_size: 1000

exporters:
  # debug:
  #   verbosity: detailed
  nop: {}
  
service:
  telemetry:
    logs:
      level: "info"

  pipelines:
    metrics:
      receivers: [otlp]
      processors: [batch]
      # exporters: [debug]
      exporters: [nop]

    logs:
      receivers: [otlp]
      processors: [batch]
      # exporters: [debug]
      exporters: [nop]

    traces:
      receivers: [otlp]
      processors: [batch]
      # exporters: [debug]
      exporters: [nop]
