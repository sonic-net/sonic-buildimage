#
#  /etc/rsyslog.conf    Configuration file for rsyslog.
#
#                       For more information see
#                       /usr/share/doc/rsyslog-doc/html/rsyslog_conf.html

# Forward container logs with RELP preferred and UDP as automatic fallback,
# so this image works against hosts that listen on either omrelp/2514 or
# omfwd-udp/514.
#
# Two non-obvious requirements (rsyslog 8.2504.0):
#   * action.resumeRetryCount="0" — suspend on first failure so the UDP
#     action fires immediately. "-1" retries forever and prevents fallback;
#     ">0" only delays it.
#   * The omrelp action must NOT have its own queue.type. A per-action
#     queue moves the send to a worker thread, so the suspended flag is
#     set asynchronously after the rule engine has already evaluated
#     execOnlyWhenPreviousIsSuspended on the next action — UDP fallback
#     never fires.
#
# resumeInterval is how often rsyslog probes whether RELP is back.

#################
#### MODULES ####
#################

#
# Set a rate limit on messages from the container
#
module(load="imuxsock" SysSock.RateLimit.Interval="300" SysSock.RateLimit.Burst="20000") # provides support for local system logging
module(load="omrelp")

#$ModLoad imklog  # provides kernel logging support
#$ModLoad immark  # provides --MARK-- message capability

# provides UDP syslog reception
#$ModLoad imudp
#$UDPServerRun 514

# provides TCP syslog reception
#$ModLoad imtcp
#$InputTCPServerRun 514


###########################
#### GLOBAL DIRECTIVES ####
###########################

set $.CONTAINER_NAME=getenv("CONTAINER_NAME");

template (name="ForwardFormatInContainer" type="string" string="<%PRI%>%TIMESTAMP:::date-rfc3339% %HOSTNAME% %$.CONTAINER_NAME%#%syslogtag%%msg:::sp-if-no-1st-sp%%msg%")

*.* action(type="omrelp"
           target=`echo $SYSLOG_TARGET_IP`
           port="2514"
           action.resumeRetryCount="0"
           action.resumeInterval="30"
           Template="ForwardFormatInContainer")

*.* action(type="omfwd"
           target=`echo $SYSLOG_TARGET_IP`
           port="514"
           protocol="udp"
           action.execOnlyWhenPreviousIsSuspended="on"
           Template="ForwardFormatInContainer")

#
# Use traditional timestamp format.
# To enable high precision timestamps, comment out the following line.
#
#$ActionFileDefaultTemplate RSYSLOG_TraditionalFileFormat

# Define a custom template
$template SONiCFileFormat,"%TIMESTAMP%.%timestamp:::date-subseconds% %HOSTNAME% %syslogseverity-text:::uppercase% %$.CONTAINER_NAME%#%syslogtag%%msg:::sp-if-no-1st-sp%%msg:::drop-last-lf%\n"
$ActionFileDefaultTemplate SONiCFileFormat

#
# Set the default permissions for all log files.
#
$FileOwner root
$FileGroup adm
$FileCreateMode 0640
$DirCreateMode 0755
$Umask 0022

#
# Where to place spool and state files
#
$WorkDirectory /var/spool/rsyslog

#
# Include all config files in /etc/rsyslog.d/
#
$IncludeConfig /etc/rsyslog.d/*.conf

#
# Suppress duplicate messages and report "message repeated n times"
#
$RepeatedMsgReduction on

###############
#### RULES ####
###############
