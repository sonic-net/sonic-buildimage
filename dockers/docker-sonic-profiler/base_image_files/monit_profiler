###############################################################################
## Monit configuration for profiler container
###############################################################################
check program container_memory_profiler with path "/usr/bin/memory_checker profiler 419430400"
    if status == 3 for 10 times within 20 cycles then exec "/usr/bin/restart_service profiler" repeat every 2 cycles
