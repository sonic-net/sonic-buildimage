Index: pam_radius/pam_radius_auth.conf
===================================================================
--- pam_radius.orig/pam_radius_auth.conf
+++ pam_radius/pam_radius_auth.conf
@@ -26,7 +26,7 @@
 #
 #  The timeout field controls how many seconds the module waits before
 #  deciding that the server has failed to respond.  Timeouts MUST be
-#  between 3 and 60 seconds.  If they are outside of this range, the
+#  between 1 and 60 seconds.  If they are outside of this range, the
 #  timeouts are clamped to this range.
 #
 #  The source_ip field can be used to make the library bind the socket
Index: pam_radius/src/pam_radius_auth.c
===================================================================
--- pam_radius.orig/src/pam_radius_auth.c
+++ pam_radius/src/pam_radius_auth.c
@@ -706,8 +706,8 @@
 		/*
 		 *	Clamp the timeouts to reasonable values.
 		 */
-		if (timeout < 3) {
-			server->timeout = 3;
+		if (timeout < 1) {
+			server->timeout = 1;
 		} else if (timeout > 60) {
 			server->timeout = 60;
 		} else {
