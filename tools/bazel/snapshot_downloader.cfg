# Bazel downloader rewrite: fetch pinned Debian snapshot debs through the
# Cloudflare-backed mirror instead of snapshot.debian.org, which aggressively
# rate-limits and fails CI when several platform jobs fetch the same snapshot in
# parallel. snapshot-cloudflare.debian.org serves identical content.
rewrite snapshot.debian.org/(.*) snapshot-cloudflare.debian.org/$1
