Index: saibcm-modules/systems/linux/kernel/modules/bcm-ngknet/include/lkm/lkm.h
===================================================================
--- saibcm-modules.orig/systems/linux/kernel/modules/bcm-ngknet/include/lkm/lkm.h
+++ saibcm-modules/systems/linux/kernel/modules/bcm-ngknet/include/lkm/lkm.h
@@ -131,6 +131,16 @@
     timer_setup(timer, fn, 0)
 #endif
 
+/* Linux 6.15 removed del_timer_sync (renamed timer_delete_sync in 6.2,
+ * wrapper kept till 6.14). Linux 6.16 renamed from_timer to timer_container_of
+ * (no compat wrapper). Provide aliases for older opennsl source. */
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,15,0)
+#define del_timer_sync(t) timer_delete_sync(t)
+#endif
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,16,0)
+#define from_timer(var, cb, field) timer_container_of(var, cb, field)
+#endif
+
 #if LINUX_VERSION_CODE < KERNEL_VERSION(4,6,0)
 static inline void page_ref_inc(struct page *page)
 {
Index: saibcm-modules/systems/linux/kernel/modules/include/lkm.h
===================================================================
--- saibcm-modules.orig/systems/linux/kernel/modules/include/lkm.h
+++ saibcm-modules/systems/linux/kernel/modules/include/lkm.h
@@ -68,6 +68,7 @@
 #include <linux/interrupt.h>
 #include <linux/stat.h>
 #include <linux/sched.h>
+#include <linux/timer.h>
 #include <linux/cpumask.h>
 #if LINUX_VERSION_CODE >= KERNEL_VERSION(4,11,0)
 #include <linux/sched/signal.h>
@@ -199,4 +200,14 @@
 #define kernel_ethtool_ts_info ethtool_ts_info
 #endif
 
+/* Linux 6.15 removed del_timer_sync (renamed timer_delete_sync in 6.2,
+ * wrapper kept till 6.14). Linux 6.16 renamed from_timer to timer_container_of
+ * (no compat wrapper). Provide aliases for older opennsl source. */
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,15,0)
+#define del_timer_sync(t) timer_delete_sync(t)
+#endif
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,16,0)
+#define from_timer(var, cb, field) timer_container_of(var, cb, field)
+#endif
+
 #endif /* __COMMON_LINUX_KRN_LKM_H__ */
Index: saibcm-modules/sdklt/linux/include/lkm/lkm.h
===================================================================
--- saibcm-modules.orig/sdklt/linux/include/lkm/lkm.h
+++ saibcm-modules/sdklt/linux/include/lkm/lkm.h
@@ -117,6 +117,16 @@
     from_timer(var, context, timer_fieldname)
 #endif
 
+/* Linux 6.15 removed del_timer_sync (renamed timer_delete_sync in 6.2,
+ * wrapper kept till 6.14). Linux 6.16 renamed from_timer to timer_container_of
+ * (no compat wrapper). Provide aliases for older opennsl source. */
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,15,0)
+#define del_timer_sync(t) timer_delete_sync(t)
+#endif
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(6,16,0)
+#define from_timer(var, cb, field) timer_container_of(var, cb, field)
+#endif
+
 #ifndef setup_timer
 #define setup_timer(timer, fn, data) \
     timer_setup(timer, fn, 0)
